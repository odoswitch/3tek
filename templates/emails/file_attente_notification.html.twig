<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notification File d'Attente</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #ffc107;
        color: #333;
        padding: 20px;
        text-align: center;
        border-radius: 5px 5px 0 0;
      }
      .content {
        background-color: #f9f9f9;
        padding: 20px;
        border-radius: 0 0 5px 5px;
      }
      .info-box {
        background-color: #fff3cd;
        border-left: 4px solid #ffc107;
        padding: 15px;
        margin: 15px 0;
      }
      .highlight {
        background-color: #d4edda;
        border: 1px solid #c3e6cb;
        padding: 10px;
        border-radius: 4px;
        margin: 10px 0;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 12px;
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>⏰ File d'Attente</h1>
      <p>3Tek-Europe - Notification</p>
    </div>

    <div class="content">
      <h2>Bonjour
        {{ user.name }},</h2>

      <p>Vous avez été ajouté à la file d'attente pour le lot suivant :</p>

      <div class="info-box">
        <h3>📦 Détails du Lot</h3>
        <p>
          <strong>Nom :</strong>
          {{ lot.name }}</p>
        <p>
          <strong>Description :</strong>
          {{ lot.description|slice(0, 100) }}...</p>
        <p>
          <strong>Prix :</strong>
          {{ lot.prix|number_format(2, ',', ' ') }}
          €</p>
        <p>
          <strong>Date d'ajout :</strong>
          {{ fileAttente.createdAt|date('d/m/Y H:i') }}</p>
      </div>

      <div class="highlight">
        <h3>⏰ Délai d'Attente</h3>
        <p>Vous avez
          <strong>1 heure</strong>
          pour confirmer votre commande à partir de maintenant.</p>
        <p>
          <strong>Expire le :</strong>
          {{ fileAttente.expiresAt|date('d/m/Y H:i') }}</p>
      </div>

      <p>
        <strong>Que se passe-t-il maintenant ?</strong>
      </p>
      <ul>
        <li>Le lot sera libéré automatiquement après expiration</li>
        <li>Vous recevrez une notification si le lot devient disponible</li>
        <li>Vous pouvez annuler votre inscription à tout moment</li>
      </ul>

      <p>Connectez-vous à votre espace client pour suivre votre file d'attente.</p>
    </div>

    <div class="footer">
      <p>3Tek-Europe - Système de File d'Attente</p>
      <p>Email automatique - Ne pas répondre</p>
    </div>
  </body>
</html>

