<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 3px solid #667eea;
        }
        .content {
            padding: 30px 0;
        }
        .alert-info {
            background: #d1ecf1;
            border-left: 4px solid #0c5460;
            padding: 15px;
            margin: 20px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background: #f8f9fa;
            font-weight: bold;
        }
        .total-row {
            background: #667eea;
            color: white;
            font-size: 18px;
            font-weight: bold;
        }
        .client-info {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
        }
        .btn {
            display: inline-block;
            padding: 12px 24px;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            margin: 10px 5px;
        }
        .footer {
            text-align: center;
            padding: 20px 0;
            border-top: 1px solid #ddd;
            color: #666;
            font-size: 14px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="header">
        <img src="{{ logoUrl }}" alt="3Tek-Europe" style="max-width: 180px; height: auto;">
    </div>

    <div class="content">
        <h2>🔔 Nouvelle commande reçue</h2>
        
        <p>Bonjour {{ admin.name }},</p>

        <p>Une nouvelle commande vient d'être passée sur la plateforme 3Tek-Europe.</p>

        <div class="client-info">
            <h4 style="margin-top: 0;">Informations client :</h4>
            <p style="margin: 5px 0;"><strong>Nom :</strong> {{ client.name }} {{ client.lastname }}</p>
            <p style="margin: 5px 0;"><strong>Email :</strong> {{ client.email }}</p>
            <p style="margin: 5px 0;"><strong>Téléphone :</strong> {{ client.phone }}</p>
            <p style="margin: 5px 0;"><strong>Entreprise :</strong> {{ client.office }}</p>
        </div>

        <h4>Détails de la commande :</h4>

        <table>
            <thead>
                <tr>
                    <th>N° Commande</th>
                    <th>Article</th>
                    <th style="text-align: center;">Qté</th>
                    <th style="text-align: right;">Total</th>
                </tr>
            </thead>
            <tbody>
                {% for commande in commandes %}
                <tr>
                    <td>{{ commande.numeroCommande }}</td>
                    <td>{{ commande.lot.name }}</td>
                    <td style="text-align: center;">{{ commande.quantite }}</td>
                    <td style="text-align: right;">{{ commande.prixTotal|number_format(2, ',', ' ') }} €</td>
                </tr>
                {% endfor %}
                <tr class="total-row">
                    <td colspan="3" style="text-align: right;">TOTAL :</td>
                    <td style="text-align: right;">{{ total|number_format(2, ',', ' ') }} €</td>
                </tr>
            </tbody>
        </table>

        <div class="alert-info">
            <strong>📌 Action requise :</strong><br>
            Une fois le paiement reçu et vérifié, vous devez valider la commande dans l'interface d'administration pour réduire automatiquement le stock.
        </div>

        <div style="text-align: center; margin: 30px 0;">
            <a href="{{ url('admin') }}" class="btn">
                Accéder à l'administration
            </a>
        </div>

        <p><strong>Rappel du processus :</strong></p>
        <ol>
            <li>Vérifier la réception du virement ({{ total|number_format(2, ',', ' ') }} €)</li>
            <li>Se connecter à l'interface d'administration</li>
            <li>Aller dans "Commandes"</li>
            <li>Changer le statut de "En attente" à "Validée"</li>
            <li>Le stock sera automatiquement réduit</li>
        </ol>
    </div>

    <div class="footer">
        <p>
            <strong>3Tek-Europe - Administration</strong><br>
            Cet email a été envoyé automatiquement.
        </p>
    </div>
</body>
</html>
